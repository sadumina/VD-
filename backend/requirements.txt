fastapi==0.115.0
uvicorn[standard]==0.30.6
motor==3.5.1          # async MongoDB driver
pydantic==2.9.2       # data validation (comes with FastAPI but keep it pinned)
python-dotenv==1.0.1  # optional if you want env vars for Mongo URL
